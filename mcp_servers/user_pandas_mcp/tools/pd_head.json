{
  "name": "pd_head",
  "description": "查看 pandas DataFrame 前 N 行数据，支持指定行数、列过滤、显示格式控制，返回结构化表格和摘要统计。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string"
      },
      "n": {
        "type": "integer",
        "default": 10
      }
    },
    "required": [
      "name"
    ],
    "description": "查看 pandas DataFrame 前 N 行数据，支持指定行数、列过滤、显示格式控制，返回结构化表格和摘要统计。"
  }
}