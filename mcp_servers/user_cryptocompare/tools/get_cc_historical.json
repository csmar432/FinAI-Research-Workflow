{
  "name": "get_cc_historical",
  "description": "获取加密货币历史价格",
  "inputSchema": {
    "type": "object",
    "properties": {
      "symbol": {
        "type": "string",
        "description": "加密货币或代币代码（如 BTC/ETH）"
      },
      "start_date": {
        "type": "string",
        "description": "起始日期（格式：YYYYMMDD 或 YYYY-MM-DD）"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期（格式：YYYYMMDD 或 YYYY-MM-DD）"
      },
      "interval": {
        "type": "string",
        "default": "day",
        "description": "数据间隔（如 1d/1h/5m/1m）"
      }
    },
    "required": [
      "symbol",
      "start_date",
      "end_date"
    ],
    "description": "获取加密货币历史价格"
  }
}