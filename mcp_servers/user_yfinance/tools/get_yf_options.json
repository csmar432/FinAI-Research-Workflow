{
  "name": "get_yf_options",
  "description": "获取股票期权数据（到期日、行使价、未平仓合约量、成交量、隐含波动率）。使用 Yahoo Finance API，无需 API Key。用于期权定价、波动率交易和风险对冲研究。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ticker": {
        "type": "string",
        "description": "股票代码（如 AAPL、SPY）",
        "required": true
      },
      "date": {
        "type": "string",
        "description": "期权到期日（YYYY-MM-DD，不填则返回所有到期日）"
      }
    },
    "required": [
      "ticker"
    ],
    "description": "获取股票期权数据。使用 Yahoo Finance API，无需 API Key。"
  }
}