{
  "name": "fs_batch_rename",
  "description": "批量重命名文件（支持替换模式）。\\n\\n",
  "inputSchema": {
    "type": "object",
    "properties": {
      "properties": {
        "type": "string",
        "description": "操作参数字典（JSON 格式）"
      }
    },
    "required": [],
    "description": "批量重命名文件（支持替换模式）。"
  }
}