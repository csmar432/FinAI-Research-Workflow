{
  "name": "fs_hash",
  "description": "计算文件哈希值（MD5/SHA256）。\\n\\n",
  "inputSchema": {
    "type": "object",
    "properties": {
      "properties": {
        "type": "string",
        "description": "操作参数字典（JSON 格式）"
      }
    },
    "required": [],
    "description": "计算文件哈希值（MD5/SHA256）。"
  }
}