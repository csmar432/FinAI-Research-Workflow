{
  "name": "fs_watch",
  "description": "监控目录变化（新增/修改/删除文件）。\\n\\n",
  "inputSchema": {
    "type": "object",
    "properties": {
      "properties": {
        "type": "string",
        "description": "操作参数字典（JSON 格式）"
      }
    },
    "required": [],
    "description": "监控目录变化（新增/修改/删除文件）。"
  }
}