{
  "name": "fs_tree",
  "description": "生成目录树结构（类似tree命令）。\\n\\n",
  "inputSchema": {
    "type": "object",
    "properties": {
      "properties": {
        "type": "string",
        "description": "操作参数字典（JSON 格式）"
      }
    },
    "required": [],
    "description": "生成目录树结构（类似tree命令）。"
  }
}