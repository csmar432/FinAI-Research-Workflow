{
  "name": "get_context7_by_arxiv",
  "description": "通过 ArXiv ID 获取论文全文（使用 Context7）。Context7 将 ArXiv 论文和 DOI 全文索引，提供完整的上下文理解。无需 API Key。优势在于返回完整论文内容（而非只有摘要），可用于深度理解论文方法细节和支持追问具体内容。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "arxiv_id": {
        "type": "string",
        "description": "ArXiv ID（如 2301.12345）",
        "required": true
      },
      "max_results": {
        "type": "integer",
        "description": "最大返回全文片段数",
        "default": 5
      }
    },
    "required": [
      "arxiv_id"
    ],
    "description": "通过 ArXiv ID 获取论文全文。使用 Context7，无需 API Key。"
  }
}